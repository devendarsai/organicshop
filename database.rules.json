{
  "rules": {
    ".read": true,
    ".write": true,
    "users": {
      "$uid": {
        ".read": "auth.uid === $uid",
        ".write": "auth.uid === $uid",
        "isAdmin": { ".read": "auth.uid === $uid" }
      }
    }
  }
}
